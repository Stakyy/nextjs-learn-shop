import Heading from "../Heading";
import styles from "../../styles/Footer.module.scss";

const Footer = () => (
  <footer className={styles.footer}>
    <Heading tag="h3" text="МБП" />
  </footer>
);

export default Footer;
